# ---- 签名密钥：绝不入库 ----
# 应用签名唯一的意义是证明「这个更新来自原作者」；私钥一旦公开即永久失效，
# 任何人都能签出与官方包同名同版本同签名者的 APK。
# 官方构建用的密钥放在仓库外的 ../_keys/，这里再拦一道，防止误提交。
keystore/
_keys/
*.keystore
*.jks
*.p12
*.pem
*.pk8
*.pass
lineage.bin

# ---- 构建产物（build.sh 的输出，不入库）----
build/
dist/*.apk.idsig
# dev 构建变体（TASK-003，`bash tools/build.sh --dev`）：只用于本机开放调试入口，
# 绝不发布。它落在 dist/ 里是为了和发布包同一处、便于对照；这里拦一道防止误提交。
# ⚠️ 发版门禁 1 扫的是**文件系统**的 dist/*.apk（不是 git），所以它仍会 FAIL 提醒你删掉。
dist/*-dev-debug.apk
# 本地增量校验用的状态文件，只对本机构建有意义
dist/.last_version_code

# ---- Gradle：本工程不使用 Gradle ----
# 构建走 tools/build.sh（免 Gradle，仅需 JDK 17 + Android SDK build-tools + Python）。
# 建项初期曾试过 AGP 7.4.2，引导失败后转向手写构建脚本 —— 这才是"无 Gradle"的来历。
# 那批残留配置（app/build.gradle 停在 versionCode 2，且用 signingConfigs.debug 假冒发布
# 签名）已于 2026-09-25 永久删除。
# 下面几条保留为**护栏**：将来若有人再引入 Gradle，确保这些文件不会误进仓库。
build.gradle
settings.gradle
gradle.properties
app/build.gradle
.gradle/
local.properties

# ---- 编辑器 / 系统 ----
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ---- 发版门禁临时比对产物（TASK-001）：用完即删，绝不入库 ----
_release_gate_tmp/
