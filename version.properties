# 微读墨记 · 版本号唯一来源
#
# 发版流程：
#   1. 改这里的 VERSION_NAME / VERSION_CODE / NOTES
#   2. bash tools/build.sh
#   3. git add -A && git commit && git push
#   4. 在 GitHub 上给这次提交打 tag：v<VERSION_NAME>（例：v0.5.0）
#
# 为什么要打 tag：App 的下载地址用 tag 引用（jsDelivr / raw 对 tag 是永久缓存、
# 且打 tag 后立刻可用）。若改用分支引用，CDN 缓存会导致新文件读不到。
#
# VERSION_CODE 必须是**递增整数**，App 靠它判断有没有新版。
# 不要改小、不要复用 —— 否则老版本会认为自己比新版本还新，永远收不到更新。
VERSION_NAME=0.8.0
VERSION_CODE=30

# 更新说明：一行短句，发版后会显示在 App 的"检查更新"提示里。
# 🔴 V0.6.1 的两句各有来由，删任何一句都会让用户踩坑：
#    ①「实验室」= TASK-000 新增（设置页第三段页签，设备能力自检 · 只读）；
#    ②「重开无障碍」= TASK-005 把无障碍服务挪进 a11y 子包、组件名变了
#       （.CardA11yService → .a11y.CardA11yService）；系统按旧组件名解绑，
#       覆盖升级后桌面卡片会消失，用户须手动重开一次。实测见 验证记录/47。
# 🔴 V0.7.0 一句的来由：TASK-009 + TASK-010 两个 ELauncher 桌面 bug 修复
#    （设置隐藏页让位失效 + 3 页桌面下翻到 P2/P3 卡片误显示遮挡图标），
#    均已真机验收（验证记录/53、56；无障碍组件名未变 ⇒ 覆盖升级无需重开）。
# 🔴 V0.8.0 一句的来由：TASK-011 + TASK-012 两项桌面卡片交互新功能
#    （仅一页模式默认关 = 现状不变；刷新绑定默认周+本书），
#    均已真机验收（验证记录/60、61；审查 62；组件名与权限未变 ⇒ 覆盖升级无需重开）。
NOTES=新增两项设置：仅一页模式（确认桌面只有一页时开启，翻页时卡片不再让位）、刷新绑定（点刷新时勾选的本周/本月/本书数据一并更新，默认本周+本书）

# 签名密钥放在**仓库外**的私有目录（默认 ../_keys），绝不入库、绝不公开。
# 缺密钥时 build.sh 会直接失败 —— 这是有意设计，不要改成回退调试签名。
# 签名策略与轮换链的来龙去脉见 README 与 验证记录/29、30、31。
